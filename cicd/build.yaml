.build:
  stage: build
  rules:
    - if: '$CI_COMMIT_BRANCH && $CI_PIPELINE_SOURCE == "push" && $CI_OPEN_MERGE_REQUESTS || $CI_COMMIT_TAG != null'
      when: never
    - if: '$CI_COMMIT_REF_NAME != "main"'

build-stdx:
  image: registry.gitlab.com/kobolds-io/stdx/zig-base-image:latest
  extends: .build
  script:
    - cd /workspace
    # we are just building the examples. We really are just making sure that they compile :)
    - zig build examples
